// Auto-generated by sdk-mcp-gen — do not edit by hand
// API: MCP Gateway API
// package: client
export type Json = null | boolean | number | string | Json[] | { [k: string]: Json };
export interface ClientOptions { baseUrl?: string; fetchImpl?: typeof fetch }
export function createClient(opts: ClientOptions = {}) {
  const baseUrl = (opts.baseUrl || "").replace(/\/$/, "");
  const f = opts.fetchImpl || fetch;
  async function request(method: string, path: string, body?: unknown) {
    const res = await f(baseUrl + path, {
      method,
      headers: body ? { "content-type": "application/json" } : undefined,
      body: body ? JSON.stringify(body) : undefined,
    });
    if (!res.ok) throw new Error(method + " " + path + " -> " + res.status);
    const text = await res.text();
    return text ? JSON.parse(text) : null;
  }
  async function getHealth(args: Record<string, unknown> = {}) {
    let path = "/health";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getReady(args: Record<string, unknown> = {}) {
    let path = "/ready";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getOpenApi(args: Record<string, unknown> = {}) {
    let path = "/openapi.json";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getMetrics(args: Record<string, unknown> = {}) {
    let path = "/metrics";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function toolsList(args: Record<string, unknown> = {}) {
    let path = "/tools/list";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function mcpToolsList(args: Record<string, unknown> = {}) {
    let path = "/mcp/tools/list";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function toolsCall(args: Record<string, unknown> = {}) {
    let path = "/tools/call";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function mcpToolsCall(args: Record<string, unknown> = {}) {
    let path = "/mcp/tools/call";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function mcpStreamableGet(args: Record<string, unknown> = {}) {
    let path = "/mcp";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function mcpJsonRpc(args: Record<string, unknown> = {}) {
    let path = "/mcp";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function mcpSessionDelete(args: Record<string, unknown> = {}) {
    let path = "/mcp";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("DELETE", path + (qs ? "?" + qs : ""));
  }
  async function auditQuery(args: Record<string, unknown> = {}) {
    let path = "/audit";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function auditExport(args: Record<string, unknown> = {}) {
    let path = "/audit/export";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function adminReload(args: Record<string, unknown> = {}) {
    let path = "/admin/reload";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function adminListTenants(args: Record<string, unknown> = {}) {
    let path = "/admin/tenants";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function adminGetTenant(args: Record<string, unknown> = {}) {
    let path = "/admin/tenants/{tenantId}";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function adminListSessions(args: Record<string, unknown> = {}) {
    let path = "/admin/sessions";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function adminDeleteSession(args: Record<string, unknown> = {}) {
    let path = "/admin/sessions/{id}";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("DELETE", path + (qs ? "?" + qs : ""));
  }
  async function adminGetConfig(args: Record<string, unknown> = {}) {
    let path = "/admin/config";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function adminListWebhooks(args: Record<string, unknown> = {}) {
    let path = "/admin/webhooks";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getAdminAuditCsv(args: Record<string, unknown> = {}) {
    let path = "/admin/audit.csv";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getAdminAuditMd(args: Record<string, unknown> = {}) {
    let path = "/admin/audit.md";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getAdminAuditHtml(args: Record<string, unknown> = {}) {
    let path = "/admin/audit.html";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function getAdminAudit(args: Record<string, unknown> = {}) {
    let path = "/admin/audit";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    const q = new URLSearchParams();
    for (const [k, v] of Object.entries(args)) {
      if (path.includes("/" + encodeURIComponent(String(v)))) continue;
      if (v !== undefined && v !== null) q.set(k, String(v));
    }
    const qs = q.toString();
    return request("GET", path + (qs ? "?" + qs : ""));
  }
  async function adminRotateTenantToken(args: Record<string, unknown> = {}) {
    let path = "/admin/tenants/{tenantId}/rotate";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  async function adminRotateTenantTokenByBody(args: Record<string, unknown> = {}) {
    let path = "/admin/tenants/rotate";
    path = path.replace(/\{([^}]+)\}/g, (_, k) => encodeURIComponent(String(args[k] ?? "")));
    return request("POST", path, args);
  }
  return {
    getHealth,
    getReady,
    getOpenApi,
    getMetrics,
    toolsList,
    mcpToolsList,
    toolsCall,
    mcpToolsCall,
    mcpStreamableGet,
    mcpJsonRpc,
    mcpSessionDelete,
    auditQuery,
    auditExport,
    adminReload,
    adminListTenants,
    adminGetTenant,
    adminListSessions,
    adminDeleteSession,
    adminGetConfig,
    adminListWebhooks,
    getAdminAuditCsv,
    getAdminAuditMd,
    getAdminAuditHtml,
    getAdminAudit,
    adminRotateTenantToken,
    adminRotateTenantTokenByBody,
  };
}
